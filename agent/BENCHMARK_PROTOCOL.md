# Benchmark protocol

Every benchmark needs an owning domain, unique slug, solver/analysis metadata, entrypoint, expected artifacts, physical validator, normalized status, and compact reference. Start new work at `NOT_RUN`; update status only from an attributable execution.
