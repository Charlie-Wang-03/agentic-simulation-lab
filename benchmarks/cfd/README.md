# Cfd

Required product families: Fluent. The manifest currently registers 21 entries (PASS=21).

```bash
agentic-sim list --domain cfd
agentic-sim info cfd
agentic-sim run cfd --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
