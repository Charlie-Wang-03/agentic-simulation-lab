# Porous Geomechanics

Required product families: Fluent, MAPDL. The manifest currently registers 12 entries (PASS=12).

```bash
agentic-sim list --domain porous_geomechanics
agentic-sim info porous_geomechanics
agentic-sim run porous_geomechanics --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
