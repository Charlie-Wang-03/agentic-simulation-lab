# Dem

Required product families: Rocky. The manifest currently registers 12 entries (BLOCKED=1, PASS=11).

```bash
agentic-sim list --domain dem
agentic-sim info dem
agentic-sim run dem --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
