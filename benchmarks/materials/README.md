# Materials

Required product families: Mechanical, MAPDL. The manifest currently registers 8 entries (PASS=8).

```bash
agentic-sim list --domain materials
agentic-sim info materials
agentic-sim run materials --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
