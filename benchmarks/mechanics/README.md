# Mechanics

Required product families: Mechanical, MAPDL. The manifest currently registers 12 entries (NOT_RUN=3, PASS=9).

```bash
agentic-sim list --domain mechanics
agentic-sim info mechanics
agentic-sim run mechanics --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
