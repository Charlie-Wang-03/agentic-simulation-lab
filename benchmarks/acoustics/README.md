# Acoustics

Required product families: Mechanical, MAPDL, Fluent. The manifest currently registers 11 entries (PASS=11).

```bash
agentic-sim list --domain acoustics
agentic-sim info acoustics
agentic-sim run acoustics --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
