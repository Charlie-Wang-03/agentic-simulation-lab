# Multiphysics

Required product families: Fluent, Mechanical, System Coupling. The manifest currently registers 10 entries (FAIL=1, PASS=9).

```bash
agentic-sim list --domain multiphysics
agentic-sim info multiphysics
agentic-sim run multiphysics --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
