# Sph

Required product families: Rocky. The manifest currently registers 15 entries (BLOCKED=2, PASS=13).

```bash
agentic-sim list --domain sph
agentic-sim info sph
agentic-sim run sph --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
