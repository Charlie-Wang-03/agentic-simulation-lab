# Thermal

Required product families: Mechanical, MAPDL. The manifest currently registers 7 entries (PASS=7).

```bash
agentic-sim list --domain thermal
agentic-sim info thermal
agentic-sim run thermal --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
