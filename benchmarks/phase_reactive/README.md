# Phase Reactive

Required product families: Fluent, MAPDL, reduced-order Python. The manifest currently registers 14 entries (FAIL=2, PASS=12).

```bash
agentic-sim list --domain phase_reactive
agentic-sim info phase_reactive
agentic-sim run phase_reactive --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
