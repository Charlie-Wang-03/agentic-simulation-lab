# Electromagnetics

Required product families: AEDT. The manifest currently registers 12 entries (BLOCKED=1, PASS=11).

```bash
agentic-sim list --domain electromagnetics
agentic-sim info electromagnetics
agentic-sim run electromagnetics --suite --dry-run
```

Statuses are evidence-backed and use the project vocabulary. Read `references/suite_summary.json` for the compact aggregate; generated solver data belongs under `artifacts/`.
