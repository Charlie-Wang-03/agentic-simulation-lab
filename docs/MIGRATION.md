# Migration

The refactor inventoried the flat workspace, mapped scripts into 11 domains, moved rather than copied large outputs, sanitized compact suite summaries, and generated manifests only from attributable evidence. `migration_receipt.json` records source-to-destination moves; local legacy data is kept under ignored artifacts. The public project name is `agentic-simulation-lab`.
