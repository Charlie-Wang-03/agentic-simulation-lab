# 智能体优先工作流

传统 GUI 流程容易把关键状态隐藏在点击操作和工程文件中；智能体优先流程把意图与证据显式化：

```text
人的意图 → 清单发现 → 智能体推理 → CLI 执行 → 求解器 → 物理验证 → 产物
```

智能体负责协调，不替代数值求解器，也不替代工程判断。先运行 `agentic-sim list`，再用 `info` 查看案例、用 `doctor` 诊断环境，并对准确命令做试运行；获得授权后才启动求解器。应阅读数值检查，并把失败保留为有价值的证据。
