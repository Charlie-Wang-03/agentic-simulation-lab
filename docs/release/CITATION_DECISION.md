# Citation identity decision

Checked: 2026-08-20

The only confirmed public maintainer identifier is `Charlie-Wang-03`. No personal name, email address, ORCID, affiliation, or guessed GitHub no-reply address is published.

`CITATION.cff` represents `Charlie-Wang-03` as a CFF 1.2.0 **entity** using its required `name` field. This is preferable to inventing `given-names` or `family-names`. The file was validated successfully with `cffconvert 2.0.0` against Citation File Format schema 1.2.0 in an isolated project-artifact environment on 2026-08-20.

The version remains `0.1.0`, but no release date, DOI, tag, or archived release is claimed because none exists. The maintainer should revalidate the file if public identity or release metadata changes.
